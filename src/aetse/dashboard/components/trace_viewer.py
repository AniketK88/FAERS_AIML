"""Agent trace viewer component — step-by-step execution path visualization.

Renders the agent_trace list for a selected case with:
- Timeline view of each LangGraph node execution
- Latency breakdown per node
- Confidence scores at each gate
- Retry indicators and human-review flags
"""
