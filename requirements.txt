# =============================================================================
# AET-SE: Root requirements — Streamlit Cloud only reads this file.
# Keep it identical to src/aetse/dashboard/requirements.txt so uv
# doesn't produce "More than one requirement" conflicts.
#
# For the full ML pipeline (local dev), use:
#   pip install -r requirements-pipeline.txt
# =============================================================================
streamlit==1.45.1
plotly==6.1.2
duckdb>=1.1.0
pandas>=1.4.0,<3
